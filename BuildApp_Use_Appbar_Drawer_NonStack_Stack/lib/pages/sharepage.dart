import 'package:flutter/material.dart';

class sharepages extends StatelessWidget {
  const sharepages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('share page'),),
      body: Center(child: Text('this is a share page'),),
    );
  }
}